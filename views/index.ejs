<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400..700&display=swap"
      rel="stylesheet"
    />
    <title>Valentine's Surprise</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #ffebeb;
            font-family: Arial, sans-serif;
            font-family: 'Dancing Script', cursive;
        }
        .headline {
            font-size: 24px;
            text-align: center;
            padding: 0 20px;
        }
        .container {
            max-width: 800px;
            padding: 0 20px;
            text-align: center;
        }
        .container2 {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            margin-top: 20px;
        }
        .envelope {
            position: relative;
            width: 200px;
            height: 150px;
            background: #d32f2f;
            border-radius: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            cursor: pointer;
        }
        .envelope::before {
            content: "";
            position: absolute;
            top: 0;
            width: 0;
            height: 0;
            border-left: 100px solid transparent;
            border-right: 100px solid transparent;
            border-bottom: 75px solid #b71c1c;
        }
        .envelope::after {
            content: "";
            position: absolute;
            bottom: 0;
            width: 0;
            height: 0;
            border-left: 100px solid transparent;
            border-right: 100px solid transparent;
            border-top: 75px solid #b71c1c;
        }
        .card {
            position: absolute;
            width: 160px;
            height: 100px;
            background: white;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            font-size: 18px;
            font-weight: bold;
            color: #d32f2f;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease-in-out;
        }
        .card:hover {
            transform: translateY(-10px);
        }
        .open-button {
            margin-top: 10px;
            padding: 8px 15px;
            background: #d32f2f;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .open-button:hover {
            background: #b71c1c;
        }

        @media only screen and (max-width: 600px) {
            .headline {
                font-size: 20px;
            }
            .container {
                padding: 0 10px;
            }
            .envelope {
                width: 150px;
                height: 120px;
            }
            .card {
                width: 130px;
                height: 80px;
                font-size: 16px;
            }
        }

        @media only screen and (min-width: 601px) and (max-width: 800px) {
            .headline {
                font-size: 22px;
            }
            .container {
                padding: 0 15px;
            }
            .envelope {
                width: 180px;
                height: 140px;
            }
            .card {
                width: 150px;
                height: 90px;
                font-size: 17px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="headline">Hey Miss Sarakshi, yeah its me your boyfriend😄. I know this is a bit surprising for you but I got something for you this valentine. I hope you find this message well</h1>
        <div class="container2">
            <div class="envelope" onclick="openMessagePage()">
                <div class="card">
                    Open Me ❤️
                    <button class="open-button" type="button">Open</button>
                </div>
            </div>
        </div>
    </div>
    <script>
        function openMessagePage() {
            window.location.href = "/message";
        }
    </script>
</body>
</html>
